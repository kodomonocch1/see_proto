//! SEE core (stub): schema-aware JSON compression trait surfaces.
//! Replace with the real binary core when ready.

pub struct SeeCodec;

impl SeeCodec {
    pub fn version() -> &'static str {
        "0.1.0-stub"
    }
    // pub fn encode(/* data: &[u8] */) -> Vec<u8> { unimplemented!() }
    // pub fn lookup_exists(/* key: &str */) -> bool { unimplemented!() }
}
